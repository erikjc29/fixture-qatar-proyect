import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'fixture-2022',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
